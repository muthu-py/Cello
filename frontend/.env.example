# Leave empty in development to use Vite proxy to localhost backends.
# Or set absolute URLs if the API runs elsewhere.
# VITE_INVENTORY_API=http://localhost:3000
# VITE_FINANCE_API=http://localhost:5002
